/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.githubusercontent.com', 'imgur.com', 'i.imgur.com', 'i.redd.it', 'preview.redd.it'],
    },
  }

export default nextConfig;
